default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: storage/development.sqlite3

test:
  # Use DATABASE_URL para evitar fallback para o usuário do sistema "root"
  url: <%= ENV['DATABASE_URL'] %>

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>